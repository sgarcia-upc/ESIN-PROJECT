enum Status {FREE, DELETED, BUSY};

struct node_hash {
    string _k;
    Valor _v;
    Status _ss;
    nat next;
};

node_hash *_taula = NULL;  // taula amb les parells <k,v>
nat _M;             // mida de la taula
nat _excedents;     // mida de la taula d'excedents
nat _quants;        // no d'elements guardats al diccionari

int hash(const string &k) const throw();
int next_prime(int num) const;
bool is_prime(const int num) const;
float sqroot(const int m) const;

void rehash();
